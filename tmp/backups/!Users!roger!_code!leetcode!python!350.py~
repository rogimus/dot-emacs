from collections import Counter

nums1 = [4,9,5]
nums2 = [9,4,9,8,4]

if len(nums1) > len(nums2):
    count = Counter(nums1)
    n = nums2
else:
    count = Counter(nums2)
    n = nums1

for x in n:
    if count.get(x,0) > 0:
        count[x] -= 1
    else:
        n.remove(x)
